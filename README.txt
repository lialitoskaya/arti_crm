Arti CRM v41 Ozon return message links

Это НЕ полный проект. Не распаковывать поверх всей папки проекта.

Заменить только:
- app/static/app.js
- app/static/styles.css
- app/static/index.html

Что добавлено:
- номера возвратов Ozon теперь становятся кликабельными по тому же принципу, что и номера отправлений;
- номер берётся из структурированных данных сообщения, как раньше: приоритетно из data[0], fallback — context.order_number;
- случайные цифры из текста сообщения не распознаются как заказ/возврат;
- если номер заканчивается на -R + цифры, например 12345678-R1, CRM открывает раздел возвратов;
- если номер заканчивается обычным числовым суффиксом через тире, например 12345678-1, CRM открывает отправление.

Ссылка для возвратов:
https://seller.ozon.ru/app/returns/main?tab=90&sort=StatusDate&sortingType=Desc&filters=[{%22single%22:{%22field%22:%22ReturnNumber%22,%22value%22:%22{order_number}%22}}]&__rr=1

Что сохранено:
- твой текущий index.html из загруженного файла;
- твой текущий styles.css из загруженного файла;
- v40 бесконечная подгрузка списка чатов;
- v39 фикс приближения в поиске;
- v38 поиск по сообщениям;
- одна папка статики: только app/static, без app/static/static.

Версия статики:
v41-ozon-return-message-links-20260629
