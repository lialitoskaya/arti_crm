Arti CRM v54-chat-theme-direction-fix-20260701

Это НЕ полный проект. Не распаковывать поверх всей папки проекта.

Заменить только:
- app/static/styles.css
- app/static/index.html

Что исправлено:
- светлая тема принудительно возвращает светлый список чатов и светлые пузырьки сообщений;
- тёмная тема оставляет список чатов и диалог в тёмных цветах;
- hover/active в тёмной теме становятся светлее;
- hover/active в светлой теме становятся чуть темнее;
- CSS использует html:not([data-theme="dark"]) как fallback, поэтому светлая тема работает даже до инициализации JS.

Функционал не трогался: app.js и backend не изменялись.
